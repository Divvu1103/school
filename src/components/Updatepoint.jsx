import React from "react";

const Updatepoint = ({ name }) => {
  return (
    <div className="text-blue-700 p-2 font-semibold border-b-4 border-black">
      <i class="fa-solid fa-arrow-right"></i>
      {name}
    </div>
  );
};

export default Updatepoint;
